# 다음에 시킬 것들 (프롬프트 모음)

그대로 복사해서 붙여넣으면 되는 형태로 적어 뒀다. 위에 있을수록 급하다.
각 항목의 "왜"는 실제로 코드를 확인하고 쓴 것이지 짐작이 아니다.

| 항목 | 상태 |
| --- | --- |
| 1. 백업·복원 버튼 | ✅ 완료 |
| 2. 모의고사 오답 → 오답노트 | ✅ 완료 |
| 3. 모의고사 점수 → 성적 자동 반영 | ✅ 완료 |
| 4. 청해·청독해 문항 집필 | ⬜ **남아 있음** |
| 5. 모의고사 기술 세션 AI 채점 | ✅ 완료 |
| 6. 지문 후리가나 토글 | ✅ 완료 |
| 7. 오프라인에서 열리게 (PWA) | ✅ 완료 |

남은 것은 **4번 하나**다. 청해·청독해는 문항을 직접 집필해야 하는 일이라
코드가 아니라 콘텐츠 작업이다.

---

## 1. 백업·복원 버튼 ✅ 완료

> **왜 급한가** — 모든 학습 기록이 이 브라우저의 localStorage에만 있다. 브라우저 데이터를
> 지우거나, 시크릿 모드로 열거나, 다른 기기로 옮기면 전부 사라진다. `exportData()`와
> `importData()` 함수는 `src/lib/storage/localStorage.ts`에 이미 있는데 **화면 어디에도
> 연결돼 있지 않다.** 지금은 백업할 방법이 없다.

```
설정 화면(/settings)에 백업·복원 기능을 붙여줘.

- "백업 파일 내보내기" 버튼: exportData()로 JSON을 만들어 eju-backup-YYYY-MM-DD.json 으로 다운로드
- "백업 파일 불러오기" 버튼: 파일 선택 → importData()로 복원. 덮어쓰기 전에 현재 데이터가
  사라진다는 걸 반드시 확인받을 것 (덱 몇 개, 카드 몇 장이 지워지는지 숫자로 보여주기)
- 복원 실패 시(형식이 다른 파일 등) 앱이 죽지 않고 오류 메시지만 뜨게
- 마지막 백업 날짜를 기억해 두고, 30일 넘게 백업을 안 했으면 대시보드에 알림 띄우기

localStorage가 유일한 저장소라는 점과, 브라우저 데이터를 지우면 복구가 안 된다는 점을
설정 화면에 분명히 적어줘.
```

**한 것** — `SettingsView`에 백업·복원 섹션. `exportBackup()`이 `eju-backup-YYYY-MM-DD.json`을
내려주고 `settings.lastBackupAt`을 같이 남긴다. 복원은 파일을 고른 즉시 덮어쓰지 않고
`summarizeBackup()`으로 훑어서 **덱·카드·오답·시험기록·작문 개수를 "지금 vs 백업 파일"로
표에 놓고** 확인을 받는다. 형식이 다른 파일이면 오류 메시지만 뜨고 기존 데이터는 그대로다.
확인 창 안에 "먼저 지금 데이터 백업" 버튼도 뒀다.
마지막 백업이 30일을 넘기거나 한 번도 없으면 대시보드 맨 위에 `/settings`로 가는 경고가 뜬다.

---

## 2. 모의고사 오답이 오답노트로 안 들어감 ✅ 완료

> **왜** — `src/components/review/ReviewView.tsx`가 `sourceType === "card"`만 걸러낸다.
> 모의고사에서 틀린 문항은 `examAttempts[].results`에 쌓이지만 오답노트에는 안 나타난다.
> 정작 다시 봐야 할 건 이쪽인데.

```
모의고사에서 틀린 문항이 오답노트(/review)에 나오게 해줘.

- 채점할 때 틀린 문항을 mistakes에 남기기 (sourceType에 "mock" 추가)
- 오답노트에서 그 문항을 펼치면 지문·발문·선택지·해설이 다 보이게. 번역 토글도 그대로 쓸 수 있게
- "다시 풀기"로 그 문항만 모아서 재시험
- 맞히면 resolved 처리

기존 카드 오답과 섞이지 않게 탭이나 필터로 나눠줘.
저장 용량이 걱정되니 문항 내용을 복사해 넣지 말고 paperId+questionId만 저장할 것.
```

**한 것** — `MockRunner`가 제출할 때 틀린 문항마다
`addMistake("mock", mockMistakeId(paperId, sectionId, questionId))`를 부른다. 맞힌 문항은
같은 키로 resolved 처리되므로 같은 회차를 다시 풀면 오답이 저절로 정리된다.
`ReviewView`는 **카드 / 문제 / 모의고사** 3개 탭으로 나눴고, 모의고사 탭은
`resolveMockMistake()`로 원본을 되찾아 지문(`PassageView`)·발문·선택지·해설(`QuestionView`)을
그대로 펼친다 — 번역·후리가나 토글도 모의고사 화면과 똑같이 동작한다.
"틀린 문항만 다시 풀기"는 그 문항들만 모아 재시험하고, 맞히면 `resolveMistake("mock", id)`.
저장되는 건 `paperId:sectionId:questionId` 문자열뿐이고 문항 본문은 복사하지 않는다.

---

## 3. 모의고사 점수가 성적 화면에 자동 반영 안 됨 ✅ 완료

> **왜** — `src/components/scores/ScoresView.tsx`는 `examRecords`(손으로 입력하는 것)만
> 본다. 모의고사를 풀면 `examAttempts`에 쌓이는데 둘이 이어져 있지 않아서, 점수 추이를
> 보려면 같은 걸 두 번 입력해야 한다.

```
모의고사를 채점하면 성적(/scores)에 자동으로 기록되게 해줘.

- examAttempts를 EJU 환산 점수로 바꿔서 성적 그래프에 같이 표시
  (일본어 독해는 정답률 × 200점, 다른 과목은 정답률 × 200점 기준)
- 손으로 넣은 기록과 모의고사 자동 기록을 구분해서 보여줄 것 (아이콘이나 색으로)
- 같은 회차를 여러 번 풀었으면 최신 것만 추이에 넣고, 나머지는 접어두기
- 환산 점수는 어디까지나 추정이라는 걸 화면에 적어줄 것
```

**한 것** — `ScoresView`가 `mergeScoreSeries(examRecords, examAttempts)`를 본다.
`buildAutoMockRecords()`가 회차별로 가장 최근 응시만 골라 정답률 × 만점으로 환산하므로,
같은 회차를 여러 번 풀어도 추이에는 한 점만 들어간다. 나머지 응시 기록은
"모의고사 응시 기록 전체" 섹션에 접어 뒀다.
차트의 점 모양으로 구분한다 — 직접 입력은 ●, 모의고사 자동 환산은 ■(속 빈 사각형).
목록에서도 자동 기록은 보라색 배경 + "모의 자동 · 추정" 배지가 붙고 삭제 버튼이 없다
(원본은 `examAttempts`이므로 여기서 지울 대상이 아니다).
화면 맨 위에 "JASSO가 배점·등화를 공개하지 않아 실제 점수와 다르다"는 안내를 고정해 뒀다.

---

## 4. 청해·청독해 모의고사 문항 채우기 ⬜ 남아 있음

> **왜** — 4회차 모두 청해·청독해 섹션이 "준비 중"으로 잠겨 있다. 스키마와 화면은
> 이미 다 만들어져 있어서 `questions` 배열만 채우면 바로 열린다.
> EJU 일본어 400점 중 200점이 청해·청독해다.

```
모의고사 제1회의 청독해(12문항)·청해(15문항)를 집필해서 채워줘.

- src/lib/mock/papers/japanese01/index.ts 의 listeningChartPlaceholder()를 실제 섹션으로 교체
- 청독해: 표나 그래프를 보면서 듣는 형식. MockPassage의 table 필드를 쓸 것
- 청해: 음성만. 스크립트는 passage(kind: "script")에 넣고 풀 때는 가려두기
- 음성은 기존 speak() (Web Speech API) 사용, 실전처럼 1회 재생 제한
- 문항마다 한국어 해설과 topicId(jp-chodoku-*, jp-cho-*) 붙일 것

다 만들면 npm run check:mock 으로 정답 분포 편중까지 검증해줘.
```

**아직 안 함.** 화면·스키마·채점·오답노트 연결은 전부 준비돼 있어서 `questions` 배열만
채우면 바로 열린다. 다른 항목과 달리 코드 문제가 아니라 문항을 직접 써야 하는 일이다.

---

## 5. 모의고사 기술 세션에도 AI 채점 붙이기 ✅ 완료

> **왜** — `/writing`에는 AI 채점이 붙었는데, 모의고사 안의 기술 세션
> (`src/components/mock/WritingRunner.tsx`)은 아직 체크리스트 자가채점만 한다.
> 같은 기능이 두 벌로 갈라져 있다.

```
모의고사 기술 세션에서도 AI 채점을 받을 수 있게 해줘.

- WritingRunner에 /api/grade-writing 연결하고 GradeResultPanel 재사용
- /writing 의 자동 점검(analyze.ts)도 같이 붙여서 두 화면의 기준을 통일
- 체크리스트 자가채점은 남겨두되, AI 채점 결과와 나란히 보이게
- 채점 결과를 writingEntries에 저장해서 /writing 의 과거 작성물에서도 볼 수 있게

두 화면이 같은 코드를 쓰도록 공통 컴포넌트로 빼줘. 지금은 글자 수 세는 법까지 따로다.
```

**한 것** — 공통 코드를 세 개로 뺐다.
`useWritingGrade()`(채점 상태 + `/api/grade-writing` 호출 + 오프라인 차단),
`gradeToEntryFields()`(채점 결과를 `writingEntries`에 넣을 형태로),
`WritingChecks` + `useWritingAnalysis()`(analyze.ts 자동 점검 패널).
`WritingRunner`는 이제 자동 점검 → AI 채점(`GradeResultPanel`) → 자가 채점 체크리스트를
차례로 보여주고, 저장하면 AI 점수·축별 코멘트·첨삭까지 `writingEntries`에 들어간다.
그래서 `/writing`의 "과거 작성물"에서 모의고사 답안의 채점 결과도 그대로 열린다.
`WritingView`도 같은 훅으로 갈아 끼웠다 — 글자 수 세는 법(`analyze.ts`의 `countChars`)이
양쪽에서 하나가 됐다.

---

## 6. 지문 후리가나 토글 ✅ 완료

> **왜** — 일본어가 약한 상태에서 독해 지문을 읽을 때 한자 읽는 법을 모르면 사전을
> 일일이 찾아야 한다. 번역 토글은 있는데 읽기 토글이 없다.

```
모의고사 지문에 후리가나(읽는 법) 토글을 붙여줘.

- 번역 버튼 옆에 "후리가나" 버튼. 누르면 한자 위에 히라가나가 ruby 태그로 붙게
- 읽는 법 데이터는 kuroshiro 같은 라이브러리 대신, 이미 있는 단어장 데이터
  (jlptN5KanjiWords 등)에서 매칭되는 것만 붙이는 방식으로 시작
- 매칭 안 되는 한자는 그냥 두기 (틀린 읽기를 붙이는 것보다 낫다)
- 번역과 마찬가지로 기본은 꺼짐

실전에는 후리가나가 없으니, 채점 후 복습할 때 쓰라는 안내를 적어줘.
```

**한 것** — `PassageView`의 번역 버튼 옆에 "후리가나" 토글. 켜면 `annotateFurigana()`가
`<ruby><rt>` 조각을 만들어 `FuriganaText`가 렌더한다. 읽기 데이터는 `jlptN5FullWords`·
`jlptN4Words`에서 표기가 그대로 맞는 것만 최장일치로 붙이고, 사전에 없는 한자는 그냥 둔다
(틀린 읽기를 찍는 것보다 빈칸이 낫다). 기본은 꺼짐이고, 켜면
"실전 시험지에는 후리가나가 없다 — 채점 후 복습할 때 쓰라"는 안내가 지문 위에 뜬다.
오답노트의 모의고사 탭도 같은 컴포넌트를 쓰므로 복습 화면에서 바로 켤 수 있다.

---

## 7. 오프라인에서 열리게 (PWA) ✅ 완료

> **왜** — `public/manifest.json`은 있는데 서비스워커가 없다. 지하철처럼 인터넷이
> 끊기는 곳에서는 앱이 아예 안 열린다. 단어 암기는 그런 데서 하는 건데.

```
서비스워커를 붙여서 오프라인에서도 열리게 해줘.

- 앱 셸과 단어 데이터를 캐시. 한 번 연 뒤에는 비행기 모드에서도 학습 가능하게
- 번역(DeepL)과 AI 채점은 온라인이 필요하니, 오프라인이면 버튼을 비활성화하고
  "인터넷 연결이 필요하다"고 안내
- 새 버전이 배포되면 "새 버전이 있다. 새로고침" 알림 띄우기
- 캐시 용량이 localStorage 할당량을 잡아먹지 않게 주의 (Cache Storage는 별도지만 확인 필요)

next-pwa 같은 패키지를 쓰든 직접 짜든 상관없다. 다만 개발 중에 캐시 때문에
수정이 반영 안 되는 일이 없게 개발 모드에서는 꺼줘.
```

**한 것** — 패키지 없이 `public/sw.js`를 직접 짰다(50줄 남짓).
앱 셸(`/`, `/study/today`, `/study/japanese`, `/study/library`, `/review`, `/mock`)을 설치할 때
미리 받아 두고, 문서 요청은 network-first(새 배포를 바로 받고 실패하면 캐시), 해시가 붙는
`/_next/static/*`은 cache-first다. **`/api/*`는 캐시에 손대지 않는다** — 번역·AI 채점이
옛 응답을 되돌려 주면 원인 찾기가 지옥이 된다. 캐시 이름에 버전을 박고 activate에서 옛 캐시를
지우며, 정적 자산은 300개로 제한해 Cache Storage가 무한정 늘지 않게 했다.
`ServiceWorkerRegister`는 `NODE_ENV === "production"`이고 localhost가 아닐 때만 등록하므로
개발 중에는 캐시가 아예 안 생긴다. 새 워커가 대기 상태가 되면 "새 버전이 있습니다 · 새로고침"
배너가 뜨고, 누르면 `SKIP_WAITING` → `controllerchange`에서 한 번만 새로고침한다.
`useOnline()` 훅으로 오프라인이면 번역 버튼과 AI 채점 버튼을 비활성화하고
"인터넷 연결이 필요합니다"를 띄운다. 이미 캐시된 번역은 오프라인에서도 열린다.

---

## 그 밖에 (급하지 않음)

- **딕테이션 문장 추가** — 지금 120문장. 레벨3(강의체)을 40 → 80으로 늘리면 실전 대비가 는다.
- **모의고사 5·6회차** — 독해 25문항씩. 회차가 많을수록 좋다.
- **단어장 검색** — 보관함에 덱이 45권이라 이름으로 찾는 기능이 있으면 편하다.
- **학습 통계** — 요일별·시간대별 학습량, 정답률 추이 같은 것.
- **DB 도입** — 폰과 PC에서 진도를 같이 보고 싶어지면. 지금은 기기별로 따로다.
  Supabase나 Neon 계정을 먼저 만들어야 한다.
