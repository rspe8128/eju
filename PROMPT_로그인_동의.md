# 커서에 줄 프롬프트 — 로그인 전 약관 동의

> 사용법: `---` 아래 코드블록만 그대로 복사해서 커서에 붙여넣을 것.

---

## 왜 필요한가

`/privacy`, `/terms` 페이지는 이미 만들었고 로그인 화면 하단에 링크도 있다. 하지만
**체크 없이도 "Google로 계속하기" 버튼이 바로 눌린다.** 개인정보보호법상 요구하는 건
페이지를 어딘가에 걸어 두는 게 아니라, **수집·이용에 동의했다는 것을 수집 시점에
확인받고 그 사실을 남기는 것**이다. 지금 구조로는 나중에 "동의한 적 없다"고 하면
반박할 기록이 없다.

Google OAuth라 회원가입과 로그인이 한 버튼으로 합쳐져 있으므로, 동의는 그 버튼을
누르기 **직전**에 받는다.

---

```
로그인 화면(LoginView.tsx)에 약관 동의 체크박스를 추가하고, 동의 시각을 서버에 남겨줘.

## 1. 로그인 화면

- "개인정보처리방침과 이용약관에 동의합니다" 체크박스 하나. /privacy, /terms 링크는
  체크박스 라벨 안에 그대로 넣을 것(새 탭에서 열리게, target="_blank")
- 체크하기 전에는 "Google로 계속하기" 버튼을 disabled 처리
- 체크 상태는 컴포넌트 로컬 state로 충분. 새로고침하면 다시 체크해야 해도 된다
  (동의는 매번 확인받는 게 아니라 "이 로그인 시도 전에 확인했다"는 뜻이면 됨)

## 2. profiles 테이블에 컬럼 추가

alter table public.profiles
  add column if not exists terms_agreed_at timestamptz;

RLS는 이미 있는 profiles_update_own 정책을 그대로 쓰면 된다(본인만 갱신 가능).
마이그레이션 SQL 파일은 supabase/ 폴더에 phase4_terms_consent.sql 같은 이름으로 추가하고,
supabase/setup_all.sql에도 반영할 것(기존 파일들이 어떻게 구성돼 있는지 보고 같은 방식으로).

## 3. 동의 시각 기록

- 체크박스를 켠 상태로 로그인 버튼을 눌렀을 때의 시각을, OAuth 리다이렉트가 끝나고
  돌아온 뒤(/profile 페이지 등 세션이 확정된 시점)에 profiles.terms_agreed_at에 저장한다.
  이미 값이 있으면 덮어쓰지 않는다(최초 동의 시각을 보존).
- 리다이렉트 전후로 상태를 넘길 방법이 필요하면 sessionStorage에 짧게 플래그를
  남겼다가, 세션 확정 후 그 플래그를 읽어서 DB에 쓰고 지우는 방식으로 해도 된다.
  과하게 복잡한 서버 콜백 구조를 새로 만들지 말 것 — 기존 auth 흐름(getUser 등)만으로
  충분하다.

## 4. 이미 로그인해 있던 기존 세션 처리

이 기능을 넣기 전에 이미 로그인한 사용자는 terms_agreed_at이 비어 있다. 그런 사용자가
다음에 앱을 열었을 때:
- 전체 화면을 막는 모달까지는 필요 없다. AppShell 어딘가(또는 /profile)에서
  terms_agreed_at이 null이면 "약관에 동의해 주세요" 배너를 띄우고, 배너 안의 동의
  버튼을 누르면 그 시점 시각으로 채운다 정도로 가볍게 처리
- 이 프로젝트는 아직 실제 이용자가 거의 없는 출시 전 단계이므로, 여기에 과도한
  공수를 들이지 말 것. 배너 하나면 충분하다

## 스타일

- 기존 패턴 따를 것(LoginView.tsx, AdminUsersView.tsx의 supabase 호출 방식 참고)
- 다크모드 대응, 모바일 반응형
- any 타입 금지, 작업 끝나면 npx tsc --noEmit 통과 확인
```
