# 커서에 줄 프롬프트 — 개인정보처리방침 · 이용약관

> 사용법: 아래 "왜 필요한가"는 참고만 하고, `---` 아래 코드블록만 그대로 복사해서 커서에 붙여넣을 것.

---

## 왜 필요한가

Google 로그인으로 계정 정보를 받고(`profiles` 테이블: 표시 이름, 아바타, 목표 대학/학과, 목표 시험일),
학습 기록 전체(`study_data` 테이블: 덱·카드·오답·성적·작문·딕테이션)를 Supabase DB에 저장한다.
작문 답안은 채점을 위해 OpenRouter 경유로 AI 모델에, 지문·문항은 번역을 위해 DeepL에 전송된다.

개인정보를 다루는 서비스는 이용자 규모와 무관하게 개인정보처리방침이 있어야 하고,
Google OAuth도 테스트 사용자 100명을 넘겨 정식 서비스로 쓰려면 심사에서 요구한다.
지금은 `/privacy`, `/terms` 페이지 자체가 없다.

**주의**: 이 프롬프트는 문서 초안을 만드는 것이지 법률 자문이 아니다. 완성된 문서를
공개하기 전에 실제 이용자에게 노출해도 되는 내용인지 한 번은 직접 읽어볼 것 —
특히 아래 "채워야 할 빈칸" 부분.

---

```
개인정보처리방침(/privacy)과 이용약관(/terms) 페이지를 만들어줘.

## 배경 (문서 내용에 정확히 반영할 것 — 지어내지 말고 아래 사실만 쓸 것)

- 로그인: Google OAuth (Supabase Auth 경유). 이메일, 이름, 프로필 사진을 받는다.
- DB(Supabase)에 저장되는 것:
  - profiles 테이블: 표시 이름, 아바타 URL, 목표 대학, 목표 학과, 목표 시험일
  - study_data 테이블: 학습 기록 전체를 JSON 하나로 저장(단어장, 카드, 오답, 성적,
    작문, 딕테이션 등) — "학습에 관련된 모든 기록"이라고 뭉뚱그려 설명해도 된다
- 로컬 저장: 로그인하지 않아도 브라우저 localStorage에 같은 데이터가 저장된다
  (오프라인 학습용). 로그인하면 DB와 동기화된다.
- 제3자 제공(전부 "처리위탁"으로 서술 — 서비스 운영에 필요한 것이지 마케팅 목적이 아님):
  - Google — 로그인 인증
  - Supabase — 인증·데이터베이스 호스팅
  - Vercel — 웹사이트 호스팅
  - OpenRouter(경유하여 Anthropic Claude 등 AI 모델) — 작문(記述) 답안 AI 채점.
    사용자가 작성한 일본어 작문 원문이 채점을 위해 전송된다
  - DeepL — 지문·문항·답안 번역
- 쿠키: Supabase Auth의 세션 쿠키만 사용. 광고·추적 쿠키 없음
- 분석 도구·광고 트래커: 없음
- 회원 탈퇴: 계정 삭제 시 profiles·study_data 레코드도 함께 삭제된다(cascade).
  탈퇴 방법은 아직 화면이 없으면 "설정에서 문의" 정도로 남겨 두고,
  실제 탈퇴 기능은 이 작업 범위에 넣지 않는다

## 페이지

- src/app/privacy/page.tsx, src/app/terms/page.tsx
- 기존 /guide 페이지처럼 정적 텍스트 위주 페이지로 (components/guide/GuideView.tsx 형식 참고)
- 개인정보처리방침에 포함할 항목: 수집하는 개인정보 항목, 수집 목적, 보유·이용 기간,
  제3자 제공/처리위탁 현황(위 목록 그대로), 이용자의 권리(열람·정정·삭제 요청 방법),
  안전성 확보 조치, 시행일, 문의처
- 이용약관에는 서비스 이용 조건, 이용자의 의무, 서비스 제공자의 책임 범위(무료 개인
  학습 도구이며 시험 결과를 보장하지 않는다는 점), 데이터 손실에 대한 면책(로컬 저장이
  주 저장소가 아니고 DB 동기화는 보조 수단임을 명시), 약관 변경 안내
- **만 14세 미만 이용 제한 문구를 넣을 것** — "이 서비스는 만 14세 이상만 이용할 수
  있습니다" 정도로 간단히. (법정대리인 동의 플로우까지는 이번 작업 범위 아님)
- 문의처 이메일은 실제 값 대신 `[CONTACT_EMAIL]` 같은 플레이스홀더로 남겨서
  내가 직접 채워 넣을 수 있게 할 것 — 임의로 이메일 주소를 지어내거나 추측하지 말 것

## 접근 권한 (중요 — 빠뜨리기 쉬움)

이 앱은 미들웨어(src/lib/supabase/middleware.ts)가 로그인 안 한 사용자를 전부 /login으로
리다이렉트한다. /privacy, /terms는 로그인 안 해도 보여야 한다(Google 심사·잠재 이용자가
로그인 전에 읽을 수 있어야 하므로). PUBLIC_PATHS에 "/privacy", "/terms"를 추가해줘.

## 링크 연결

- /login 화면 하단에 두 페이지로 가는 작은 링크 추가
- /settings 화면 맨 아래에도 같은 링크 추가
- src/lib/nav.ts의 "정보 · 설정" 그룹에는 넣지 마 — 사이드 메뉴가 이미 복잡하다는
  피드백이 있었다. 그냥 페이지 하단 링크로 충분하다

## 스타일

- 기존 페이지처럼 한국어, 다크모드 대응(dark: 클래스), 모바일 반응형
- 새 상태관리·라이브러리 도입 금지, 기존 패턴만 따를 것
- 작업 끝나면 npx tsc --noEmit 통과 확인
```
